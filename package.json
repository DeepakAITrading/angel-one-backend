/*
File: backend/package.json
Description: This file defines the project, its scripts, and its dependencies.
The `npm install` command uses this file to install express, axios, cors, and dotenv.
*/
{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend server for Angel One data viewer",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "angel-one",
    "api",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
```
# File: .gitignore (in the project's root directory)
# Description: This file tells Git which files and folders to ignore.
# It's crucial for security and for keeping the repository clean.

# Node.js dependencies folder
# This folder can be very large and should be generated by `npm install`
# on the server, not stored in version control.
backend/node_modules/

# Local environment variables file
# This file contains your secret API key and MUST NOT be committed to GitHub.
backend/.env
